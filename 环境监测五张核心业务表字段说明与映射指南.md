# 环境监测五张核心业务表字段说明与映射指南

> 编制日期：2026-08-21  
> 适用范围：实验室物联监控子模块的前端字段设计、平台注册 SQL、厂家数据接入、查询统计及后续联调。  
> 结构依据：[核心业务表与表结构.md](核心业务表与表结构.md)；业务边界依据：[开发指导与数据关联边界.md](开发指导与数据关联边界.md)。

## 一、说明口径

字段解释分为三类：

| 标记 | 含义 |
| --- | --- |
| 已确认 | 来自数据库注释、主键约束、已确认表关系或已经固定的页面映射 |
| 业务候选 | 根据字段名称、类型和现有业务用途整理，可用于方案设计，但正式写入或状态翻译前仍需业务方确认 |
| 待确认 | 当前没有数据库注释或有效样例支撑，不能自行赋予固定代码含义 |

本指南不把字段名称推测当作数据库事实。尤其是 `ITEMTYPE`、`FTYPE`、`FMODE`、`IFOK`、`FIFMONITOR`、`STATUS` 等代码字段，取得正式字典前应保留原始值。

## 二、五张表的业务分工

| 表名 | 业务角色 | 主键 | 页面主要用途 |
| --- | --- | --- | --- |
| `HTLIS.LIS_LIBDEF` | 实验室/检验室档案及实验室温湿压要求 | `LIBSEQ` | 实验室对象、所属科室、位置、温湿压条件一览 |
| `HTLIS.LP_TBC_INSTFILE` | 仪器设备业务主档 | `INSTID` | 仪器对象、编号、名称、型号、实验室归属、机构隔离和档案信息 |
| `HTLIS.LP_TBC_CIRCUDEFM` | 环境监测项目定义 | `CIRCUITEMID` | 项目名称、单位、通用合格值、默认值和有效状态 |
| `HTLIS.LIS_MCIRCSLOG` | 一次环境/仪器监测记录主表 | `CIRCSLOGSEQ` | 监测时间、监测对象、记录人员、整次记录状态 |
| `HTLIS.LIS_DCIRCSLOG` | 一次监测中的项目值明细表 | `CIRCSLOGSEQ + CIRCUITEMID` | 项目值、单位快照和项目级历史判定 |

固定关系：

```text
LIS_LIBDEF.LIBSEQ
  ├─ LP_TBC_INSTFILE.LIBSEQ
  └─ LIS_MCIRCSLOG.LIBSEQ

LP_TBC_INSTFILE.INSTID
  └─ LIS_MCIRCSLOG.INSTID

LIS_MCIRCSLOG.CIRCSLOGSEQ
  └─ LIS_DCIRCSLOG.CIRCSLOGSEQ

LP_TBC_CIRCUDEFM.CIRCUITEMID
  └─ LIS_DCIRCSLOG.CIRCUITEMID
```

## 三、`HTLIS.LIS_LIBDEF` 字段说明

| 字段 | 类型/约束 | 字段说明 | 页面与开发映射 | 确认程度 |
| --- | --- | --- | --- | --- |
| `LIBSEQ` | `VARCHAR2(20)`，主键，非空 | 检验室/实验室唯一代码 | 实验室稳定键使用 `LAB:<LIBSEQ>`；关联仪器和监测主表 | 已确认 |
| `DDPTNO` | `VARCHAR2(20)` | 数据库注释为“检验室代码”；与 `LIBSEQ` 的业务区别仍需确认 | 可作为检验室业务编码候选，不作为主键 | 部分确认 |
| `LIBNO` | `VARCHAR2(20)` | 默认所属检验科码，数据库注释指出可关联 `HlDpt.DDptNo` | 所属科室候选；机构隔离前必须验证部门关系 | 已确认用途，关联待验证 |
| `LIBNAME` | `VARCHAR2(80)` | 检验室名称 | 页面实验室名称、对象名称、筛选项 | 已确认 |
| `LIBADD` | `VARCHAR2(60)` | 检验室地址 | 所属区域、位置或地址展示 | 已确认 |
| `DFDEL` | `VARCHAR2(1)` | 删除标志：`0` 未删除、`1` 已删除 | 主数据查询应排除已删除实验室 | 已确认 |
| `FRESPID` | `VARCHAR2(80)` | 负责人工号 `EMPID` | 负责人展示或后续关联人员表 | 已确认 |
| `FLOWTEMPER` | `NUMBER(5,2)` | 实验室温度下限 | 环境条件一览中的温度下限 | 业务含义已确认 |
| `FTOPTEMPER` | `NUMBER(5,2)` | 实验室温度上限 | 环境条件一览中的温度上限 | 业务含义已确认 |
| `FLOWHUMIDITY` | `NUMBER(5,2)` | 实验室湿度下限 | 环境条件一览中的湿度下限 | 业务含义已确认 |
| `FTOPHUMIDITY` | `NUMBER(5,2)` | 实验室湿度上限 | 环境条件一览中的湿度上限 | 业务含义已确认 |
| `FLOWPRESSURE` | `NUMBER(8,2)` | 实验室压力/压差下限 | 环境条件一览中的压力下限；单位需业务确认 | 业务候选 |
| `FTOPPRESSURE` | `NUMBER(8,2)` | 实验室压力/压差上限 | 环境条件一览中的压力上限；单位需业务确认 | 业务候选 |
| `FIFMONITOR` | `VARCHAR2(2)` | 是否纳入环境监测的候选标志 | 可用于对象启用筛选；代码值未确认前显示原值 | 业务候选 |
| `FSORT` | `NUMBER(3)` | 排序号 | 实验室列表排序 | 已确认 |
| `CENTERX` | `NUMBER(5,2)` | 平面图中心 X 坐标候选 | 后续实验室平面图定位；当前页面非必需 | 业务候选 |
| `CENTERY` | `NUMBER(5,2)` | 平面图中心 Y 坐标候选 | 同上 | 业务候选 |
| `LIMITSX` | `NUMBER(5,2)` | 平面图 X 方向范围/半径候选 | 区域覆盖或绘图范围；算法待确认 | 待确认 |
| `LIMITSY` | `NUMBER(5,2)` | 平面图 Y 方向范围/半径候选 | 区域覆盖或绘图范围；算法待确认 | 待确认 |
| `FLOORNUM` | `NUMBER(4)` | 楼层编号候选 | 楼层筛选或空间展示 | 业务候选 |
| `RESIDENTTIME` | `NUMBER(5,2)` | 停留时间/驻留时间候选 | 当前环境页面不使用；具体单位和用途待确认 | 待确认 |

开发注意：

- `LIS_LIBDEF` 可直接提供实验室级温度、湿度和压力条件，但不能提供每台仪器的个性化结构化阈值。
- 本表没有 `FHIINO`。未确认 `LIBNO/DDPTNO` 到机构的可信关系前，不得把全库实验室直接返回给任意机构。
- 压力字段是否表示绝对压力还是房间压差、单位是 `Pa` 还是其他单位，必须由业务字典确认。

## 四、`HTLIS.LP_TBC_INSTFILE` 字段说明

### 4.1 核心标识、名称与归属

| 字段 | 类型/约束 | 字段说明 | 页面与开发映射 | 确认程度 |
| --- | --- | --- | --- | --- |
| `INSTID` | `NUMBER(8)`，主键，非空 | 仪器设备业务主键 | 仪器稳定键 `INST:<INSTID>`；关联能耗和环境记录 | 已确认 |
| `INSTNO` | `VARCHAR2(40)`，非空 | 仪器设备编号 | 页面设备编号；不可用 `FSSID` 替代 | 业务含义已确认 |
| `INSTNM` | `VARCHAR2(80)`，非空 | 仪器设备名称 | 页面设备名称、筛选和统计维度 | 业务含义已确认 |
| `INSTXH` | `VARCHAR2(80)` | 仪器型号/规格型号 | 设备详情和厂家对接识别辅助字段 | 业务候选 |
| `INSTUNIT` | `VARCHAR2(80)`，默认“台”，非空 | 计量单位 | 设备数量展示 | 业务含义已确认 |
| `LIBSEQ` | `VARCHAR2(20)` | 所属实验室代码 | 关联 `LIS_LIBDEF.LIBSEQ` | 已确认关系 |
| `STATUS` | `VARCHAR2(8)`，非空 | 仪器档案/业务状态 | 仅用于档案状态；不得翻译成在线、离线或关机 | 业务用途已确认，代码待确认 |
| `FHIINO` | `NUMBER(12)`，默认 `0`，非空 | 所属机构编号 | 仪器主档和相关数据的机构隔离 | 业务含义已确认 |
| `FUSEDPTNO` | `VARCHAR2(400)` | 使用科室编号或多科室信息候选 | 科室筛选/展示；格式需按实际值确认 | 业务候选 |
| `LOACTION` | `VARCHAR2(200)` | 安装/存放位置，字段名存在历史拼写 | 页面设备位置；不要自行改字段名 | 业务候选 |
| `PLACE` | `VARCHAR2(400)` | 场所/放置地点候选 | 可作为位置补充，优先级需与 `LOACTION` 确认 | 待确认 |
| `INSTTYPE` | `VARCHAR2(20)` | 仪器类型 | 设备类型候选；代码字典待确认 | 业务候选 |
| `FDEVICETYPE` | `VARCHAR2(4)` | 设备类型代码候选 | 与 `INSTTYPE` 的区别需确认 | 待确认 |
| `FTYPE` | `VARCHAR2(50)` | 数据库注释：仪器适用方法，`1` 前期检验、`2` 提取、`3` 扩增、`4` 电泳 | 仅用于仪器方法分类，不作为物联设备类型 | 已确认 |
| `FSPNO` | `VARCHAR2(20)` | 规格/品种/分类编号候选 | 当前物联页面不直接使用 | 待确认 |
| `FDSEQ` | `NUMBER(12)` | 关联序号候选 | 关联对象和用途未确认，不作为默认外键 | 待确认 |
| `FDIBISEQ` | `NUMBER(18)`，默认 `0` | 关联业务序号候选 | 具体关联表未确认，不用于物联关联 | 待确认 |

### 4.2 供应商、生产商和出厂信息

| 字段 | 类型 | 字段说明 | 页面与开发映射 | 确认程度 |
| --- | --- | --- | --- | --- |
| `SUPPID` | `NUMBER(8)` | 供应商内部标识候选 | 供应商关联辅助 | 业务候选 |
| `SUPPNM` | `VARCHAR2(80)` | 供应商名称 | 设备详情 | 业务候选 |
| `SUPPNO` | `VARCHAR2(40)` | 供应商编号 | 供应商关联辅助 | 业务候选 |
| `SUPPDESC` | `VARCHAR2(400)` | 供应商说明 | 设备详情备注 | 业务候选 |
| `MANUID` | `NUMBER(8)` | 生产厂家内部标识候选 | 厂家关联辅助 | 业务候选 |
| `MANUNM` | `VARCHAR2(2000)` | 生产厂家名称 | 设备详情和厂家对接 | 业务候选 |
| `MANUNO` | `VARCHAR2(40)` | 生产厂家编号 | 厂家关联辅助 | 业务候选 |
| `PROFACNO` | `VARCHAR2(20)` | 生产/厂家相关编号候选 | 与出厂编号的区别待确认 | 待确认 |
| `PRODAREA` | `VARCHAR2(80)` | 产地 | 设备详情 | 业务候选 |
| `LEAVEFACTNO` | `VARCHAR2(200)` | 出厂编号/序列号候选 | 厂家设备唯一标识辅助，不能代替 `INSTID` | 业务候选 |
| `PRODUCTION_DATE` | `DATE` | 生产日期 | 设备详情 | 业务候选 |
| `LEAVEFATDT` | `DATE` | 出厂日期候选，字段名存在历史拼写 | 设备详情 | 业务候选 |
| `ARRIVALDT` | `DATE` | 到货日期 | 采购/验收信息 | 业务候选 |

### 4.3 采购、资产和费用

| 字段 | 类型 | 字段说明 | 页面与开发映射 | 确认程度 |
| --- | --- | --- | --- | --- |
| `INSTFEE` | `NUMBER(18,2)` | 仪器购置金额候选 | 资产详情；不参与物联监测统计 | 业务候选 |
| `INSTFEETOTAL` | `NUMBER(18,2)` | 仪器总费用候选 | 资产详情；与 `INSTFEE` 的区别待确认 | 待确认 |
| `BUYDT` | `DATE` | 购买日期 | 资产详情 | 业务候选 |
| `PURCHASETYPE` | `VARCHAR2(8)` | 采购类型代码 | 需要字典翻译 | 业务候选 |
| `FASSETNO` | `VARCHAR2(100)` | 资产编号 | 页面资产信息 | 已确认 |
| `FFUNDSOURCE` | `VARCHAR2(100)` | 资金来源 | 资产信息 | 已确认 |
| `FCONTRACTNO` | `VARCHAR2(200)` | 采购合同编号 | 资产信息 | 已确认 |
| `FINVOICENO` | `VARCHAR2(200)` | 发票号 | 资产信息 | 已确认 |
| `GUARANTEELIMIT` | `DATE` | 保修期限/保修截止日期候选 | 设备保障信息 | 业务候选 |
| `WARRANTYDT` | `DATE` | 保修日期候选 | 与 `GUARANTEELIMIT` 的区别待确认 | 待确认 |

### 4.4 使用、责任、验收与停用

| 字段 | 类型 | 字段说明 | 页面与开发映射 | 确认程度 |
| --- | --- | --- | --- | --- |
| `ACCRMAN` | `VARCHAR2(8)` | 验收/计量责任人员候选 | 具体角色待确认 | 待确认 |
| `LIAB_MAN` | `VARCHAR2(8)` | 责任人编号候选 | 设备责任人展示 | 业务候选 |
| `KEEPMAN` | `VARCHAR2(40)` | 保管人 | 设备详情 | 业务候选 |
| `ACCEPTMAN` | `VARCHAR2(20)` | 验收人员 | 验收信息 | 业务候选 |
| `ACCEPTDT` | `DATE` | 验收日期 | 验收信息 | 业务候选 |
| `ACCEPTDESC` | `VARCHAR2(400)` | 验收说明 | 验收详情 | 业务候选 |
| `USEDT` | `DATE` | 启用/使用日期候选 | 设备详情 | 业务候选 |
| `STOPDATE` | `DATE` | 停用日期 | 设备详情；不得推导实时关机状态 | 业务候选 |
| `FEMPID` | `VARCHAR2(50)` | 最后操作人员或建档人员候选 | 审计展示前需确认语义 | 待确认 |
| `FOPDT` | `DATE` | 最后操作/维护时间候选 | 档案更新时间；不是设备通信时间 | 业务候选 |
| `MARK` | `VARCHAR2(40)` | 标记/标签候选 | 当前物联页面不直接使用 | 待确认 |
| `REMARK` | `VARCHAR2(100)` | 备注 | 设备详情 | 业务含义已确认 |

### 4.5 检定、校准、核查、维修和计量

| 字段 | 类型 | 字段说明 | 页面与开发映射 | 确认程度 |
| --- | --- | --- | --- | --- |
| `LASTQJHC` | `DATE` | 上次期间核查日期候选 | 质量管理信息 | 业务候选 |
| `LASTJD` | `DATE` | 上次检定/校准日期候选 | 质量管理信息 | 业务候选 |
| `FIRSTJD` | `DATE` | 首次检定日期候选 | 质量管理信息 | 业务候选 |
| `NEXTJD` | `DATE` | 下次检定日期 | 质量管理信息 | 业务候选 |
| `JDZQ` | `NUMBER(4,2)` | 检定周期数值候选 | 与周期单位配合使用 | 业务候选 |
| `JDZQDW` | `VARCHAR2(200)` | 检定周期单位 | 周期显示 | 已确认 |
| `JDZQDESC` | `VARCHAR2(40)` | 检定周期说明 | 周期补充说明 | 业务候选 |
| `JDPERIOD` | `VARCHAR2(8)` | 检定周期代码/文本候选 | 与 `JDZQ` 的区别待确认 | 待确认 |
| `JDJZF` | `NUMBER(10,2)` | 检定/校准费用候选 | 费用信息 | 业务候选 |
| `NEXTCHECKDT` | `DATE` | 下次检查日期候选 | 质量管理提醒 | 业务候选 |
| `CHECKPERIOD` | `VARCHAR2(8)` | 检查周期候选 | 字典/单位待确认 | 业务候选 |
| `CHECKABLE` | `VARCHAR2(8)` | 是否需要检查/可检查标志候选 | 代码字典待确认 | 待确认 |
| `NEXTHC` | `DATE` | 下次核查日期 | 质量管理信息 | 已确认 |
| `LASTHC` | `DATE` | 上次核查日期 | 质量管理信息 | 已确认 |
| `WHPERIOD` | `VARCHAR2(20)` | 维护周期候选 | 维护计划 | 业务候选 |
| `WHMODE` | `VARCHAR2(200)` | 维护方式候选 | 设备详情 | 业务候选 |
| `YZMODE` | `VARCHAR2(200)` | 验证方式候选 | 设备验证信息 | 业务候选 |
| `VERIFICATION` | `VARCHAR2(20)` | 验证/确认状态或方式候选 | 具体语义和代码待确认 | 待确认 |
| `NEEDMATERING` | `VARCHAR2(8)` | 是否需要计量候选 | 代码字典待确认 | 业务候选 |
| `IFJL` | `VARCHAR2(2)` | 是否计量候选 | 与 `NEEDMATERING` 的区别待确认 | 待确认 |
| `REPAIREDT` | `NUMBER(4,2)` | 维修周期或维修相关数值候选 | 类型为数值，具体单位待确认 | 待确认 |
| `BREAKDOWNRM` | `VARCHAR2(32)` | 故障/报废原因候选 | 具体业务场景待确认 | 待确认 |
| `XZYZ` | `VARCHAR2(100)` | 修正因子 | 结果修正说明；不能未经规则确认直接参与环境值换算 | 已确认 |
| `FCHKVAL` | `VARCHAR2(400)` | 检查/核查值说明候选 | 质量管理详情 | 待确认 |

### 4.6 技术能力、环境要求和控制属性

| 字段 | 类型 | 字段说明 | 页面与开发映射 | 确认程度 |
| --- | --- | --- | --- | --- |
| `TECHZB` | `VARCHAR2(200)` | 技术指标 | 仪器详情 | 业务候选 |
| `INSTZQD` | `VARCHAR2(80)` | 仪器准确度/精度描述候选 | 仪器详情 | 待确认 |
| `FINSTENNM` | `VARCHAR2(40)` | 仪器英文名称候选 | 仪器详情 | 业务候选 |
| `NUMSRCMODE` | `VARCHAR2(20)` | 数值来源/数据源模式候选 | 不得直接推断为物联采集方式 | 待确认 |
| `IFCONTRAL` | `VARCHAR2(1)` | 是否可控制候选，字段名为历史拼写 | 不能仅凭该字段开放远程控制 | 待确认 |
| `FPOWER` | `VARCHAR2(1000)` | 电源/功率要求说明候选 | 设备档案说明；不是实时功率监测值 | 业务候选 |
| `ENVIRONMENT` | `VARCHAR2(1000)` | 仪器环境要求自由文本 | 只作说明展示，不拆解成结构化阈值 | 业务用途已确认 |
| `SOFTWARE` | `VARCHAR2(200)` | 配套软件信息 | 设备详情 | 业务候选 |
| `FIFMONITOR` | `VARCHAR2(1)`，默认 `1` | 是否纳入监测候选标志 | 可作为监测对象候选；代码含义需确认 | 业务候选 |
| `PURPOSE` | `VARCHAR2(400)` | 仪器用途 | 设备详情、分类辅助 | 业务候选 |
| `TESTMINIMUM` | `VARCHAR2(32)` | 最低检测能力/检出限候选 | 设备技术信息 | 待确认 |
| `TESTTYPE` | `VARCHAR2(400)` | 检测类型候选 | 设备分类辅助 | 业务候选 |
| `TESTMETHOD` | `VARCHAR2(200)` | 检测方法 | 设备详情 | 业务候选 |
| `REPORTWAY` | `VARCHAR2(8)` | 报告方式代码候选 | 与物联监控无直接关系 | 待确认 |
| `INSTNUM` | `NUMBER` | 仪器数量候选 | 主档通常一行一台，使用前需确认 | 待确认 |
| `EXPIREDTIME` | `DATE` | 到期时间候选 | 到期提醒用途待确认 | 业务候选 |
| `PERIODUNIT` | `VARCHAR2(10)` | 周期单位候选 | 对应哪个周期字段需确认 | 待确认 |
| `PHONE` | `VARCHAR2(40)` | 联系电话候选 | 联系信息 | 业务候选 |

### 4.7 证书、注册、附件和描述

| 字段 | 类型 | 字段说明 | 页面与开发映射 | 确认程度 |
| --- | --- | --- | --- | --- |
| `INSTSELFNO` | `VARCHAR2(80)` | 仪器内部自编号候选 | 设备详情 | 业务候选 |
| `ZSBH` | `VARCHAR2(80)` | 证书编号候选 | 设备资质信息 | 业务候选 |
| `IFXIE` | `VARCHAR2(10)` | 数据库注释为“是否械字号” | 代码字典待确认 | 已确认用途 |
| `DEVICE` | `VARCHAR2(200)` | 注册证号/备案号 | 资质信息 | 已确认 |
| `TECREQ` | `VARCHAR2(200)` | 产品标准/技术要求 | 资质信息 | 已确认 |
| `PICID` | `VARCHAR2(200)` | 图片附件 ID | 附件展示 | 已确认 |
| `DEVICEID` | `VARCHAR2(200)` | 备案证附件 ID | 附件展示；不要误作设备业务主键 | 已确认 |
| `LICENSEID` | `VARCHAR2(200)` | 许可证附件 ID | 附件展示 | 已确认 |
| `TESTREPORTID` | `VARCHAR2(200)` | 检验报告附件 ID | 附件展示 | 已确认 |
| `MANUALID` | `VARCHAR2(200)` | 说明书附件 ID | 附件展示 | 已确认 |
| `IMAGEPATH` | `VARCHAR2(2000)` | 图片路径候选 | 设备媒体展示 | 业务候选 |
| `AUDIOPATH` | `VARCHAR2(2000)` | 音频路径候选 | 设备媒体展示 | 业务候选 |
| `VIDEOPATH` | `VARCHAR2(2000)` | 视频路径候选 | 仅为档案媒体路径，不代表实时视频接口 | 业务候选 |
| `FCONTENT` | `BLOB` | 二进制内容/附件候选 | 使用前确认内容格式；列表查询避免直接返回 | 业务候选 |
| `FINTRODUCE` | `VARCHAR2(2000)` | 仪器介绍 | 设备详情 | 业务候选 |
| `FDETAILS` | `VARCHAR2(4000)` | 仪器详细说明 | 设备详情 | 业务候选 |

开发注意：

- `INSTID` 是物联页面关联仪器、能耗记录和环境记录的唯一业务主键；`DEVICEID` 是备案证附件 ID，不能用作设备主键。
- `STATUS` 是仪器档案状态，不是设备在线、离线、关机或待机状态。
- `FPOWER` 是文本型档案说明，不是 `IB_TBS_DEVICEMONITORLOG.FPOWER` 的实时功率值。
- `ENVIRONMENT` 是自由文本，只能展示说明，不能解析后冒充实验室或仪器的结构化阈值。
- 本表字段多且存在历史拼写、重复语义候选。页面首期只使用 `INSTID/INSTNO/INSTNM/INSTXH/LIBSEQ/FHIINO/FUSEDPTNO/LOACTION/STATUS/ENVIRONMENT` 等已明确字段。

## 五、`HTLIS.LP_TBC_CIRCUDEFM` 字段说明

| 字段 | 类型/约束 | 字段说明 | 页面与开发映射 | 确认程度 |
| --- | --- | --- | --- | --- |
| `CIRCUITEMID` | `NUMBER(12)`，主键，非空 | 环境项目唯一标识 | 关联明细 `LIS_DCIRCSLOG.CIRCUITEMID` | 已确认 |
| `ITEMCODE` | `VARCHAR2(8)` | 环境项目编码 | 页面项目稳定业务编码优先使用该字段 | 业务含义已确认 |
| `ITEMNAME` | `VARCHAR2(20)` | 环境项目名称 | 指标名称、筛选、表头和图表标题 | 业务含义已确认 |
| `ITEMTYPE` | `VARCHAR2(12)` | 项目类型代码 | 当前只确认存在 `1/2/3`，不得擅自翻译 | 待确认代码字典 |
| `ITEMVALUE` | `VARCHAR2(18)` | 项目定义中的配置值/参考值候选 | 不是实际采集值；具体用途待确认 | 待确认 |
| `MEASUREWORD` | `VARCHAR2(40)` | 项目计量单位 | 图表纵坐标、列表单位；历史记录优先使用明细单位快照 | 业务含义已确认 |
| `OKVALUE1` | `VARCHAR2(18)` | 通用合格值边界 1 | 可显示项目通用要求；是否严格为下限需字典确认 | 部分确认 |
| `OKVALUE2` | `VARCHAR2(18)` | 通用合格值边界 2 | 可显示项目通用要求；是否严格为上限需字典确认 | 部分确认 |
| `VALUEDEF` | `VARCHAR2(18)` | 默认值候选 | 只用于定义/录入默认值，不作为采集值 | 业务候选 |
| `REMARK` | `VARCHAR2(200)` | 项目备注 | 项目详情 | 业务含义已确认 |
| `FORDER` | `NUMBER(8)` | 项目排序号 | 项目列表和展示顺序 | 业务候选 |
| `FIFVALID` | `VARCHAR2(2)` | 项目有效标志 | 当前只确认实际值出现 `1`；翻译前需确认 | 业务候选 |
| `FTESTEMPID` | `VARCHAR2(50)` | 项目定义维护人员/测试人员候选 | 不是机构字段，不得用于机构过滤 | 待确认 |

开发注意：

- `LP_TBC_CIRCUDEFM` 是公共项目定义，不含 `LIBSEQ` 或 `INSTID`，不能表示“某实验室/某仪器的个性化项目要求”。
- `ITEMVALUE/OKVALUE1/OKVALUE2/VALUEDEF` 均为字符字段。做数值比较前必须先用显式格式校验，再转换为数值。
- 项目定义中的名称和单位是当前字典值；查询历史记录时优先使用明细表保存的名称和单位快照。

## 六、`HTLIS.LIS_MCIRCSLOG` 字段说明

| 字段 | 类型/约束 | 字段说明 | 页面与开发映射 | 确认程度 |
| --- | --- | --- | --- | --- |
| `CIRCSLOGSEQ` | `NUMBER(12)`，主键，非空 | 一次环境/仪器监测记录唯一编号 | 记录主键；关联多条监测项目明细 | 已确认 |
| `TESTTIME` | `DATE` | 监测/记录时间 | 列表时间、趋势横轴、时间筛选 | 业务含义已确认 |
| `TESTMAN` | `NUMBER(16)` | 检测/监测人员编号候选 | 与人员表和 `FTESTEMPID` 的区别待确认 | 待确认 |
| `IFOK` | `VARCHAR2(1)` | 整次监测记录的合格状态原始码 | 可展示原始状态；不能替代每个项目的 `DCIRCSLOG.IFOK` | 业务用途已确认，代码待确认 |
| `LIBSEQ` | `VARCHAR2(10)` | 监测所属实验室代码 | 关联 `LIS_LIBDEF.LIBSEQ`；关联时使用 `TRIM` 统一字符语义 | 已确认关系 |
| `FTESTEMPID` | `VARCHAR2(50)` | 检测/监测人员工号候选 | 与 `TESTMAN/RECORDMAN` 的角色差异待确认；不能当机构字段 | 待确认 |
| `INSTID` | `NUMBER(8)` | 监测所属仪器编号 | 关联 `LP_TBC_INSTFILE.INSTID` | 已确认关系 |
| `FTYPE` | `VARCHAR2(8)` | 监测对象类型或业务类型原始码候选 | 当前出现 `1/2/NULL`，未确认前不得翻译 | 待确认 |
| `RECORDMAN` | `VARCHAR2(20)` | 记录人员候选 | 人工记录人或录入人展示前需确认 | 业务候选 |
| `FMODE` | `VARCHAR2(50)` | 记录/采集方式原始值候选 | 当前均为空；不能自行翻译为自动采集、人工记录或检验时抓取 | 待确认 |

开发注意：

- 一行 `MCIRCSLOG` 只表示“一次监测事件”，不包含具体温度、湿度、压力等项目值。
- 当一条主记录同时存在 `INSTID` 和 `LIBSEQ` 时，页面以仪器作为监测对象、实验室作为归属环境，只生成一个对象记录。
- 当前 33 条主记录均无法关联现有实验室和仪器档案，不能通过相近编号或名称强行归属。
- 表内没有外部报文 ID、接收时间和明确幂等键。厂家接入时必须另行确认如何防止重复上报。

## 七、`HTLIS.LIS_DCIRCSLOG` 字段说明

| 字段 | 类型/约束 | 字段说明 | 页面与开发映射 | 确认程度 |
| --- | --- | --- | --- | --- |
| `CIRCSLOGSEQ` | `NUMBER(12)`，联合主键，非空 | 所属监测主记录编号 | 关联 `LIS_MCIRCSLOG.CIRCSLOGSEQ` | 已确认关系 |
| `CIRCUITEMID` | `NUMBER(12)`，联合主键，非空 | 环境项目编号 | 关联 `LP_TBC_CIRCUDEFM.CIRCUITEMID` | 已确认关系 |
| `ITEMNAME` | `VARCHAR2(16)` | 记录发生时的项目名称快照 | 历史展示优先使用；为空时才回填当前项目定义名称 | 业务用途已确认 |
| `ITEMVALUE` | `VARCHAR2(18)` | 该次监测的项目原始值 | 环境记录主列表、详情、最新值；数值图表必须先校验再转换 | 业务用途已确认 |
| `MEASUREWORD` | `VARCHAR2(40)` | 记录发生时的计量单位快照 | 列表单位和图表纵坐标；为空时才回填定义表单位 | 业务用途已确认 |
| `IFOK` | `VARCHAR2(1)` | 该项目在该次监测中的合格状态原始码 | 项目级历史判定；不得用当前阈值重新覆盖 | 业务用途已确认，代码待确认 |

开发注意：

- 明细稳定键是 `<CIRCSLOGSEQ>:<CIRCUITEMID>`，分页选择、查看、导出和前端缓存不得使用页内序号。
- `ITEMVALUE` 是字符字段，既可能保存数值，也可能保存等级或状态文本。明细列表可以展示原值；平均值、最小值、最大值和趋势图只接纳合法数值。
- `ITEMNAME/MEASUREWORD` 是历史快照，避免项目字典后续修改导致历史展示变化。
- `IFOK` 是项目级历史结论；`MCIRCSLOG.IFOK` 是整次记录结论，二者不能混用。

## 八、页面字段映射建议

| 页面字段 | 首选来源 | 回退/补充来源 | 映射规则 |
| --- | --- | --- | --- |
| 监测对象 ID | `MCIRCSLOG.INSTID` 或 `LIBSEQ` | `INSTFILE/LIBDEF` | 仪器用 `INST:<INSTID>`；实验室用 `LAB:<LIBSEQ>` |
| 监测对象名称 | `INSTFILE.INSTNM` 或 `LIBDEF.LIBNAME` | 未关联时显示原始编号，不猜测名称 | 同时有仪器和实验室时显示仪器名称 |
| 所属实验室 | `LIBDEF.LIBNAME` | 仪器可通过 `INSTFILE.LIBSEQ` 补充 | 记录自己的 `LIBSEQ` 优先，关系冲突时需核验 |
| 所属机构 | `INSTFILE.FHIINO` | 经确认的部门—机构关系 | 环境其他四表没有 `FHIINO`，不得无范围返回全库 |
| 设备编号 | `INSTFILE.INSTNO` | 未关联时使用 `TO_CHAR(MCIRCSLOG.INSTID)` | 不使用 `DEVICEID`、`FSSID` 代替 |
| 环境项目 ID | `DCIRCSLOG.CIRCUITEMID` | `CIRCUDEFM.CIRCUITEMID` | 与主记录编号组成明细稳定键 |
| 环境项目编码 | `CIRCUDEFM.ITEMCODE` | `TO_CHAR(CIRCUITEMID)` | 项目定义缺失时保留数值 ID |
| 环境项目名称 | `DCIRCSLOG.ITEMNAME` | `CIRCUDEFM.ITEMNAME` | 历史快照优先 |
| 监测值 | `DCIRCSLOG.ITEMVALUE` | 无 | 不从项目定义 `ITEMVALUE` 或主表 `IFOK` 生成 |
| 单位 | `DCIRCSLOG.MEASUREWORD` | `CIRCUDEFM.MEASUREWORD` | 图表纵坐标必须显示具体指标名称和单位 |
| 项目状态 | `DCIRCSLOG.IFOK` | 无 | 未确认代码字典前展示原值 |
| 整次记录状态 | `MCIRCSLOG.IFOK` | 无 | 只表示整次记录，不覆盖项目状态 |
| 监测时间 | `MCIRCSLOG.TESTTIME` | 无 | Oracle `DATE` 按平台统一格式输出 |
| 采集/记录方式 | `MCIRCSLOG.FMODE` | 无 | 无值时显示“--”，不得按时间或人员推断 |
| 温湿压条件 | `LIS_LIBDEF` 对应上下限字段 | 项目通用要求取 `CIRCUDEFM.OKVALUE1/2` | 实验室个性化要求和项目通用要求分开展示 |
| 仪器环境说明 | `INSTFILE.ENVIRONMENT` | 无 | 只显示自由文本，不拆解成上下限 |

## 九、厂家或外部接口写入指导

一次完整监测建议保持以下事务语义：

```text
1 条 LIS_MCIRCSLOG
  ├─ 1 条 LIS_DCIRCSLOG：温度
  ├─ 1 条 LIS_DCIRCSLOG：湿度
  └─ N 条 LIS_DCIRCSLOG：其他已定义项目
```

写入前必须确认：

1. 监测点是实验室还是仪器，以及它与 `LIBSEQ/INSTID` 的稳定唯一映射。
2. 厂家项目编码与 `CIRCUITEMID/ITEMCODE` 的映射，不按项目名称模糊匹配。
3. 一次上报如何取得 `CIRCSLOGSEQ`，以及重复上报、断线补传和失败回滚规则；禁止使用无并发保护的 `MAX+1`。
4. `ITEMVALUE` 的原始格式、数值精度、单位和异常值表达方式。
5. `ITEMNAME/MEASUREWORD` 是否在写入时保存上报时点的历史快照。
6. `IFOK/FTYPE/FMODE` 的正式代码字典。
7. 设备时间与服务器接收时间的口径。现有五表只有 `TESTTIME`，如需同时留存接收时间和原始报文，应提供独立接入日志表。
8. 机构隔离链路。仪器记录可通过 `INSTFILE.FHIINO` 限定；实验室记录需确认部门到机构的可信关系。

## 十、当前数据现状与开发边界

截至 2026-08-20，已核验数据量为：

```text
LIS_LIBDEF        202
LP_TBC_INSTFILE   244
LP_TBC_CIRCUDEFM    3
LIS_MCIRCSLOG      33
LIS_DCIRCSLOG       0
```

因此当前可以可靠开发和展示：

- 实验室档案；
- 仪器设备档案；
- 环境项目定义；
- 实验室温度、湿度和压力条件要求。

当前不能以真实数据展示：

- 环境监测项目值；
- 最新温湿度或其他项目值；
- 环境趋势和数值统计；
- 根据环境记录生成的实时预警；
- 当前 33 条旧主记录的实验室或仪器归属。

这五张表也不提供设备心跳、采集批次执行日志、消息推送结果、预警处置过程、远程控制结果、视频联动或原始接口报文。上述功能必须使用各自独立的正式业务表或厂家接口。

## 十一、仍需业务方确认的字段清单

优先确认以下字段后，才能开放状态翻译、数据写入或高级筛选：

1. `LIS_LIBDEF.FIFMONITOR` 的代码值及压力字段单位。
2. `LP_TBC_CIRCUDEFM.ITEMTYPE/FIFVALID/ITEMVALUE/OKVALUE1/OKVALUE2/VALUEDEF/FTESTEMPID` 的准确语义。
3. `LIS_MCIRCSLOG.IFOK/FTYPE/FMODE/TESTMAN/FTESTEMPID/RECORDMAN` 的代码和人员角色。
4. `LIS_DCIRCSLOG.IFOK` 的代码值。
5. `LP_TBC_INSTFILE.STATUS/INSTTYPE/FDEVICETYPE/FIFMONITOR` 的字典，以及 `LOACTION/PLACE`、`JDZQ/JDPERIOD` 等重复候选字段的优先级。
6. `LIS_LIBDEF.LIBNO/DDPTNO` 到科室和机构的正式关系。
