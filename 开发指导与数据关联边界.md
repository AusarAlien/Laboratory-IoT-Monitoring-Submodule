# 实验室物联监控子模块开发指导与数据关联边界

本文档是本仓库后续前端页面、平台注册 SQL 和数据接入开发的统一业务边界。发生样例数据不完整、历史数据无法匹配或不同环境数据不一致时，优先保证本文确定的业务关系、数据流和机构隔离，不得根据少量历史数据临时改变字段语义。

## 1. 已确定的总体原则

- 本模块当前只开发前端页面、平台注册 SQL 和基本前后端交互，不新增 Java 接口。
- `LP_TBC_INSTFILE` 是 yncdc 功能平台的仪器设备业务主档。
- `INSTID` 是仪器设备业务主键；能耗表中的 `INSTID` 期望对应 `LP_TBC_INSTFILE.INSTID`。
- `FSSID` 是智能插座或采集终端编号，不能替代仪器业务主键。
- 页面以仪器、实验室、冷库、区域等业务对象为主，以插座、传感器、门禁控制器等物联设备为采集端。
- 真实查询使用左连接兼容无法关联档案的历史数据，不得因主档缺失丢弃采集记录。
- 当前业务范围不包含水浸设备、水浸指标和水浸模拟数据。
- Mock 只能作为显式关闭的降级代码保留，真实查询失败时不得静默切换 Mock。

## 2. 能耗业务模型

### 2.1 标识语义

| 标识 | 业务含义 | 用途 |
| --- | --- | --- |
| `LP_TBC_INSTFILE.INSTID` | 仪器设备业务主键 | 关联仪器名称、编号、型号、科室、位置和责任人 |
| 能耗表 `INSTID` | 应对应仪器设备主键 | 按仪器归集历史、状态和累计数据 |
| `FSSID` | 智能插座/采集终端编号 | 区分同一仪器下的具体采集终端 |
| `FID/FSEQ` | 数据记录主键 | 唯一标识一条历史或累计记录 |
| `FHIINO` | 所属机构 | 数据隔离和会话范围过滤 |

一台仪器可以绑定一个或多个智能插座：

```text
LP_TBC_INSTFILE.INSTID
  ├─ FSSID-1
  └─ FSSID-2
```

页面以 `INSTID` 对应的仪器为主对象，`FSSID` 作为插座编号单独展示。仪器级统计可汇总多个插座，插座级明细保留 `FSSID`。

### 2.2 表职责

| 业务表 | 职责 | 页面用途 |
| --- | --- | --- |
| `HTLIS.LP_TBC_INSTFILE` | 仪器设备主档 | 编号、名称、型号、科室、位置、责任人、档案状态 |
| `HII.IB_TBS_DEVICEMONITORLOG` | 能耗历史监测记录 | 采集明细、趋势、状态变化、历史查询 |
| `HII.IB_TBS_ITLASTINSTSTATUS` | 最新状态快照 | 最新电流、电压、功率、运行状态、最后通信时间、状态消息 |
| `HII.IB_TBS_SMARTSOCKETLOGSHOW` | 累计值展示 | 总能耗、总使用时长等累计指标 |

正确业务流：

```text
仪器设备主档
  -> 绑定 INSTID + FSSID
  -> 智能插座采集
  -> 历史记录 DEVICEMONITORLOG
  -> 最新状态 ITLASTINSTSTATUS
  -> 累计值 SMARTSOCKETLOGSHOW
  -> 状态监测、数据采集、查询统计和预警
```

### 2.3 历史数据兼容

当前能耗数据主要是其他库接入的 2025 年历史数据；当前仪器档案是相对真实的 yncdc 数据。现有样例匹配情况不能用于否定 `INSTID` 的业务关系。

已知现状仅作为兼容背景：

- 能耗历史记录：81 个设备，机构 `441200011`。
- 最新状态：82 个设备。
- 累计值：132 个设备。
- 当前样例仪器档案：10 条，机构为 `0`。
- 当前三类能耗数据与样例档案匹配数均为 0。

处理规则：

- SQL 继续按 `能耗表.INSTID = LP_TBC_INSTFILE.INSTID` 左连接。
- 匹配成功时展示正式仪器档案。
- 匹配失败时保留历史数据，编号使用能耗 `INSTID`，名称降级为“设备 + INSTID”，科室显示“未设置”。
- 不得根据名称、位置或相近编号猜测映射。
- 不得把 `FSSID` 改造成仪器档案主键。
- 不得为了适配样例数据批量改写历史 `INSTID`。

### 2.4 机构隔离

- 带 `FHIINO` 的历史表必须使用平台会话机构过滤。
- `ITLASTINSTSTATUS`、`SMARTSOCKETLOGSHOW` 没有 `FHIINO` 时，不得直接无范围返回全库数据。
- 最新状态和累计值应通过当前机构仪器档案的 `INSTID`，或通过已按机构过滤的历史设备集合限定范围。
- 前端不得自行传入或伪造机构编号覆盖平台会话。

## 3. 平台环境业务模型（后续主链路）

平台已明确环境业务使用以下 5 张表。后续环境监测页面和相关统计优先按这条业务链路建设，不再以旧温湿度采集表代替实验室、仪器、环境项目和环境记录主数据。

### 3.0 当前数据核验结论（2026-08-20）

实际 `count(*)` 结果为：`LIS_LIBDEF=202`、`LP_TBC_INSTFILE=244`、`LP_TBC_CIRCUDEFM=3`、`LIS_MCIRCSLOG=33`、`LIS_DCIRCSLOG=0`。`ALL_TABLES.NUM_ROWS` 中的 `2/10/3/33/0` 是旧统计信息，不能作为页面总数。

当前 33 条主记录均同时填写了 `LIBSEQ` 和 `INSTID`，但全部无法关联现有 `LIS_LIBDEF` 和 `LP_TBC_INSTFILE`；同时明细表没有任何项目值。因此当前数据只能支持：

- 202 个实验室档案及其中实际填写的温度、湿度、压力条件展示；
- 244 个仪器设备档案展示；
- 3 个环境项目定义展示。

当前数据不能支持：

- 环境采集值列表、最新值、温湿度趋势和数值统计；
- 有项目值的环境记录详情；
- 根据环境记录生成实时或历史超限预警；
- 把 33 条旧主记录归属到当前实验室或仪器。

33 条主记录只能视为未关联的历史记录头。不得用主表 `IFOK` 伪造细项监测值，也不得通过相近名称、编号或页内顺序强行映射。页面查询现已按主细表完成正式目标映射；只有在 `LIS_DCIRCSLOG` 产生真实明细且主档关联修复后，环境采集和环境记录页面才会具有可展示的真实监测值。

当前值域只确认 `CIRCUDEFM.ITEMTYPE` 存在 `1/2/3`、`FIFVALID=1`，主表 `FTYPE` 存在 `1/2/NULL`、`IFOK` 存在 `0/1/NULL`，且 `FMODE` 当前均为空。尚无业务字典证据解释这些代码，页面不得先行翻译成具体中文状态或采集方式。

| 业务表 | 已确认职责 | 页面用途 |
| --- | --- | --- |
| `HTLIS.LIS_LIBDEF` | 检验室/实验室定义 | 实验室名称、实验室筛选、环境监测对象归属 |
| `HTLIS.LP_TBC_INSTFILE` | 仪器设备主档 | 仪器编号、名称、实验室归属及设备类监测对象 |
| `HTLIS.LP_TBC_CIRCUDEFM` | 环境项目定义 | `CIRCUITEMID` 项目主键，包含名称、类型、单位、合格值、默认值、排序和有效标志 |
| `HTLIS.LIS_MCIRCSLOG` | 环境/仪器监测记录主表 | `CIRCSLOGSEQ` 主键，记录时间、检验室、仪器、记录方式和整次监测是否合格 |
| `HTLIS.LIS_DCIRCSLOG` | 环境/仪器监测记录细表 | 以 `(CIRCSLOGSEQ,CIRCUITEMID)` 唯一标识一次监测中的一个项目值 |

目标业务关系为：

```text
LIS_LIBDEF.LIBSEQ
  ├─ LP_TBC_INSTFILE.LIBSEQ
  └─ LIS_MCIRCSLOG.LIBSEQ

LP_TBC_INSTFILE.INSTID
  └─ LIS_MCIRCSLOG.INSTID

LIS_MCIRCSLOG.CIRCSLOGSEQ
  └─ LIS_DCIRCSLOG.CIRCSLOGSEQ

LP_TBC_CIRCUDEFM.CIRCUITEMID
  └─ LIS_DCIRCSLOG.CIRCUITEMID
```

上述关系已由表结构确定为后续查询的标准候选关系；数据库未声明外键，因此正式替换前仍需用实际数据验证孤儿记录和一对多基数。`LIS_MCIRCSLOG.LIBSEQ` 长度为 10、`LIS_LIBDEF.LIBSEQ` 长度为 20，关联时使用 `trim` 显式统一字符语义。

记录语义必须遵循以下规则：

- `LIS_MCIRCSLOG` 一行表示一次实验室或仪器环境监测，`LIS_DCIRCSLOG` 多行表示该次监测的各项目值。
- 明细稳定键为 `CIRCSLOGSEQ + CIRCUITEMID`；分页、查看和导出不能只使用 `CIRCSLOGSEQ` 或页内序号。
- `LIS_DCIRCSLOG.ITEMNAME/MEASUREWORD` 是历史记录快照，展示历史时优先使用明细值，定义表只作缺失回填。
- `LIS_DCIRCSLOG.IFOK` 是项目级历史判定，`LIS_MCIRCSLOG.IFOK` 是整次记录判定。不能用当前定义表的阈值重新覆盖历史合格状态。
- `ITEMVALUE/OKVALUE1/OKVALUE2/VALUEDEF` 均为字符字段。趋势、最小值、最大值、平均值等数值统计只能纳入通过显式数值校验的数据，禁止直接隐式 `to_number`。
- `TESTTIME` 是监测时间；`FMODE` 是记录方式候选字段；`FTYPE` 是监测对象/业务类型候选字段。二者的代码含义必须从真实数据或字典确认，不能由页面自行假定。
- 仪器记录以 `MCIRCSLOG.INSTID -> INSTFILE.INSTID` 获取设备主档；实验室记录以 `MCIRCSLOG.LIBSEQ -> LIBDEF.LIBSEQ` 获取实验室信息。二者均存在时，以仪器为监测对象、实验室为归属环境，不重复统计为两条监测记录。

环境要求分为两层：

- `LIS_LIBDEF` 已直接提供实验室级温度、湿度、压力上下限及 `FIFMONITOR`，可支撑实验室环境条件一览。
- `LP_TBC_CIRCUDEFM.OKVALUE1/OKVALUE2` 是环境项目定义中的通用合格值；该表没有 `LIBSEQ/INSTID`，不能表示每个实验室或每台仪器的个性化要求。
- `LP_TBC_INSTFILE.ENVIRONMENT` 只能作为仪器环境要求的说明文本，不能拆解成可靠的结构化上下限。
- 如需维护仪器级或非温湿压项目的对象级要求，仍需平台提供对象—项目要求关系表，或经批准新增独立关系表。

机构隔离边界：`LP_TBC_INSTFILE` 有 `FHIINO`，可对仪器记录通过 `INSTID` 限定机构；`LIS_LIBDEF/CIRCUDEFM/MCIRCSLOG/DCIRCSLOG` 均没有 `FHIINO`。在未确认 `LIS_LIBDEF.LIBNO/DDPTNO` 到机构的可信部门链路，或数据库本身为单机构隔离前，不得无范围返回全部实验室级记录。`FTESTEMPID` 名称和类型不足以证明它是机构字段，不得用于机构过滤。

页面接入顺序：

1. `维护环境条件项目`：读取 `LP_TBC_CIRCUDEFM`，字段可映射项目编码、名称、类型、单位、合格值、默认值、排序、有效状态和备注。该表无机构字段，开放新增、修改和停用前必须确认它是全平台公共字典并配置相应权限。
2. `环境条件采集`：以 `LIS_MCIRCSLOG + LIS_DCIRCSLOG` 为采集结果，关联 `LIS_LIBDEF/LP_TBC_INSTFILE/CIRCUDEFM` 展示对象和项目。
3. `环境设备条件一览表`：实验室温度、湿度、压力要求取 `LIS_LIBDEF`；项目通用要求取 `CIRCUDEFM`；当前五表不能提供结构化的仪器个性化项目要求。
4. `环境记录`：以主表为一次业务记录、细表为多项监测值，分页主键必须稳定，趋势统计从细表数值和主表时间生成。
5. `物联查询统计`：新增这条主细记录链路作为环境数据统计来源，按实验室、仪器、项目、时间等维度聚合。
6. `智能预警/监测预警`：`MCIRCSLOG.IFOK/DCIRCSLOG.IFOK` 可显示历史异常结果，但不能直接替代预警事件、通知、处置和关闭记录。

这 5 张表不能直接证明采集端在线状态、采集批次执行日志、消息推送结果或远程控制结果，相关页面仍需各自的状态、日志、预警和审计数据源。

### 3.1 旧温湿度采集兼容链路

| 业务表 | 职责 | 主要关联 |
| --- | --- | --- |
| `HII.IP_TBS_DEVICEIP` | 温湿度网络设备档案 | `FDEVICEIP` |
| `HTLIS.LIS_CHGSAMPDEPOT_REQCIRCU` | 温湿度历史记录 | `HTDEVICEIP -> FDEVICEIP` |
| `HII.IB_TBS_STANDARD` | 指标标准 | `FSDID=604` 温度，`FSDID=605` 湿度 |

边界：

- `FDEVICEIP` 是温湿度采集设备标识，不等同于仪器档案 `INSTID`。
- 温湿度设备需要映射到实验室、冷库、冰箱、培养箱、仪器等“环境监测对象”。
- 只有监测对象确实是仪器时，才关联 `LP_TBC_INSTFILE.INSTID`。
- 全局标准可用于基础判断，但每个监测对象的个性化上下限需要独立的环境条件要求数据。
- 当前温湿度记录表没有数据属于数据源现状，不得用 Mock 冒充真实采集记录。

### 3.2 旧环境预警兼容链路

| 业务表 | 职责 | 主要关联 |
| --- | --- | --- |
| `HII.IP_TBS_MONITORALARM` | 环境监测报警事件 | `FSCODE` 为历史传感器节点编码 |
| `HII.IP_TBS_DEVICEIP` | 环境监测设备档案 | `FDEVICEIP` 的 `#` 后缀为节点号 |
| `HII.IB_TBS_STANDARD` | 环境监测指标字典 | `FITEMNO -> FSDID` |

已通过实际数据确认，历史报警的设备关系不是 `FSCODE=FDEVICEIP`，而是：

```text
IP_TBS_MONITORALARM.FSCODE 去除前导 0
  -> IP_TBS_DEVICEIP.FDEVICEIP 中“#”后的节点号去除前导 0
```

当前验证数据中，`000061/000062/000064/000070` 均能唯一匹配一个 `FTYPE='HT'` 的环境设备，合计覆盖 597 条报警且无一对多关系。查询必须先按会话机构限定 `IP_TBS_DEVICEIP`，再匹配报警；不得直接使用报警表中遗留的机构编码扩大查询范围。若同一机构内节点号出现多个设备，必须视为关系不唯一并停止自动匹配，不能任取一条。

当前报警数据属于 2023 年历史记录，只有 `FALARMTYPE=2` 的异常值报警，指标为二氧化碳（601）、湿度（605）和 TVOC（606）。它可以支持历史报警列表，不得据此声称存在当前实时报警、温度报警、通知结果或完整处置流程。

后续新产生的报警应直接保存完整 `FDEVICEIP` 或稳定设备唯一键，节点尾号匹配只作为现有历史数据的兼容规则。

## 4. 门禁业务模型

```text
IB_TBS_USERINFO.USERID
  -> IB_TBS_CHECKINOUT.USERID

IB_TBS_LABAREA.SN
  -> IB_TBS_CHECKINOUT.SN
```

| 业务表 | 职责 |
| --- | --- |
| `HII.IB_TBS_USERINFO` | 门禁人员、卡号和基础权限信息 |
| `HII.IB_TBS_LABAREA` | 门禁控制器、区域和进出方向配置 |
| `HII.IB_TBS_CHECKINOUT` | 门禁出入事件记录 |

边界：

- `USERID` 是人员与出入记录的关联键。
- `SN` 是门禁控制器与区域记录的关联键。
- 门禁控制器通常属于安全设施，不强制关联仪器档案。
- 门禁记录查询不等于门禁权限下发；人员授权、控制器配置、下发结果需要相应真实业务表或接口。
- `IB_TBS_USERINFO` 在本模块中只作为门禁识别人员和出入事件的查询数据，不作为人员主档维护入口。
- 人员新增、编辑、停用、人脸维护等人员配置由外部人员管理系统负责，本模块不得重复建设写入功能。
- 门禁管理以实验室和门点远程监控为主。现有出入记录只能证明历史活动，不能推导控制器在线状态、门开关状态或视频状态。
- 远程开锁、呼叫、视频必须由可信设备网关或平台接口提供，并记录操作人、门点、指令时间和执行结果；接口缺失时页面只能展示未启用的能力入口，不能模拟成功。

## 5. 页面数据源边界

| 页面 | 主数据源 | 边界 |
| --- | --- | --- |
| 物联配置管理 | 仪器主档、检验室、物联设备配置和绑定关系 | `LP_TBC_INSTFILE/LIS_LIBDEF` 可升级设备及归属信息，但不能替代 IP、端口、周期、账号、权限配置 |
| 数据采集管理 | 环境监测主细记录、能耗历史 | `MCIRCSLOG/DCIRCSLOG` 可作为采集结果；采集执行日志仍需要独立日志数据，不能由监测记录代替 |
| 物联状态监测 | 能耗最新状态及其他设备心跳/状态 | 档案 `STATUS` 不能代替实时运行状态 |
| 物联查询统计 | 环境监测主细记录、能耗历史、累计值、检验室和仪器主档 | 环境趋势使用 `MCIRCSLOG/DCIRCSLOG`，能耗趋势使用历史表，累计结果使用累计表 |
| 监测预警管理 | 阈值规则、预警事件、处理和推送记录 | 普通监测日志不能替代预警业务表 |
| 环境条件采集 | `LIS_MCIRCSLOG/LIS_DCIRCSLOG`、检验室、仪器和项目定义 | 主表表示一次监测，细表表示该次监测的项目值；对象类型和来源必须使用真实字段 |
| 环境设备条件一览表 | `LIS_LIBDEF`、`LP_TBC_CIRCUDEFM`、`LP_TBC_INSTFILE.ENVIRONMENT` | 实验室温湿压上下限可结构化展示；项目定义是通用要求；仪器环境字段仅作说明文本，不能冒充对象级结构化阈值 |
| 环境记录 | `LIS_MCIRCSLOG/LIS_DCIRCSLOG` | 自动采集、人工记录和检验时抓取等来源必须使用业务表真实字段，不从时间或名称推断 |
| 智能预警 | 环境规则、事件、通知和处置记录 | 声光、短信、客户端、平台推送需要真实结果记录 |
| 门禁管理 | 人员、区域、控制器、出入记录及远程操作审计 | 以实验室门点监控为主；历史事件不能冒充实时状态，查询数据不能冒充远程控制能力 |
| 一卡通管理 | 外部人员主档及门禁、访客等人员活动记录 | 一卡通是人员活动数据卡和跨系统查询视图，不在本模块新增、编辑人员或维护卡片与权限 |

## 6. 禁止事项

- 禁止因为当前老数据无法匹配就把 `FSSID` 定义为仪器主键。
- 禁止使用内连接导致无档案的历史采集记录丢失。
- 禁止把仪器档案状态当作在线、离线、关机等实时状态。
- 禁止把累计值逐条求和；累计增量应按合理时间范围计算最大值减最小值。
- 禁止无机构范围查询缺少 `FHIINO` 的最新状态和累计表。
- 禁止用普通采集记录模拟采集批次日志、预警事件或推送结果。
- 禁止在页面出现水浸相关设备、指标、筛选项和 Mock 数据。
- 禁止在真实查询失败时自动切换到 Mock。

## 7. 后续开发验收检查

每次新增或修改数据接入至少检查：

1. 页面主对象是仪器/实验室/区域，还是采集终端，语义是否明确。
2. `INSTID`、`FSSID`、`FDEVICEIP`、`SN`、`USERID` 是否按本文边界使用。
3. 是否保留机构会话隔离。
4. 主档缺失时是否保留采集历史并安全降级。
5. 历史、最新状态、累计值是否使用正确的数据表。
6. 页面指标名称、单位和纵坐标是否与实际字段一致。
7. 是否误把档案状态、采集状态、预警状态混为一类。
8. 是否仍有水浸内容或隐式 Mock 回退。
9. SQL、前端字段映射和页面筛选是否一致。
10. 源码同步到本地 Conda 后是否完成实际入口验证。
11. 环境查询是否保持 `MCIRCSLOG` 一次记录与 `DCIRCSLOG` 多条项目值的主细语义，避免主表行数被细表连接重复放大。
12. 实验室、仪器、环境项目和记录的关联键是否已经过表结构、样例数据和关联基数验证。

## 8. QUERYDATA 查询结果处理

平台注册 SQL 返回 `success:false` 或 `querydata报错` 时，一律视为查询执行失败，不得在前端转换成空数组、0 条统计或 Mock 数据。只有明确的成功响应且 `data=[]` 才表示正常空结果。

当前平台查询入口存在以 `rslt` 是否为空同时判断执行状态和数据行数的问题，会把“成功但0行”和“SQL执行失败”返回成相同响应。完整问题、统一前端守卫、平台目标响应契约和排查步骤见 [QUERYDATA 空结果与执行失败处理规范](QUERYDATA_空结果与执行失败处理规范.md)。

## 9. 环境页面正式映射（2026-08-20）

已确认 `HTLIS.LIS_DCIRCSLOG` 为环境/仪器监测记录正式明细表。页面固定使用以下映射，后续厂家数据写入或外部接口落库时不得另造并行页面模型：

| 页面数据 | 正式来源 | 稳定标识/关系 |
| --- | --- | --- |
| 环境监测对象 | `LIS_LIBDEF`、`LP_TBC_INSTFILE` | 实验室 `LAB:LIBSEQ`；仪器 `INST:INSTID` |
| 环境项目定义 | `LP_TBC_CIRCUDEFM` | `CIRCUITEMID`，页面项目编码优先使用 `ITEMCODE` |
| 环境条件要求 | `LIS_LIBDEF` 的温度、湿度、压力上下限 | `LAB:LIBSEQ:项目编码` |
| 一次监测记录 | `LIS_MCIRCSLOG` | `CIRCSLOGSEQ` |
| 监测项目明细 | `LIS_DCIRCSLOG` | `CIRCSLOGSEQ:CIRCUITEMID` |
| 最新值、趋势、汇总 | `MCIRCSLOG + DCIRCSLOG` | 以明细值和主表 `TESTTIME` 生成，不使用主表伪造项目值 |

平台注册查询固定为：

- `ynjksys_hjjc_wsd_sb01q`：环境监测对象；
- `ynjksys_hjjc_wsd_bz01q`：环境项目定义；
- `ynjksys_hjjc_hjyq01q`：实验室环境条件要求；
- `ynjksys_hjjc_wsd_jl01q`：监测记录明细；
- `ynjksys_hjjc_wsd_jl02q`：监测记录汇总；
- `ynjksys_hjjc_wsd_zx01q`：最新项目值；
- `ynjksys_hjjc_wsd_qs01q`：数值趋势。

在 `LIS_DCIRCSLOG` 无数据时，记录、最新值和趋势查询应成功返回 0 行；环境项目、实验室和仪器档案仍可正常展示。厂家尚未确认 `IFOK/FTYPE/FMODE` 代码含义前，页面只展示原始状态值，不擅自翻译为正常、超限或采集方式。预警事件、推送与处置闭环继续使用独立预警业务表，不由监测明细代替。

## 10. 当前修改措施与后续优化计划

### 10.1 已完成的代码与映射

- 将环境监测对象切换为 `LIS_LIBDEF + LP_TBC_INSTFILE`，使用 `LAB:LIBSEQ` 和 `INST:INSTID` 稳定标识。
- 将环境项目切换为 `LP_TBC_CIRCUDEFM`，项目选择、项目名称、单位和图表纵坐标由真实项目定义及记录快照驱动，不再写死温湿度两项。
- 将环境条件采集、环境记录、最新值、趋势和汇总统一映射到 `LIS_MCIRCSLOG + LIS_DCIRCSLOG`，列表保留非数值原始值，趋势与数值统计只接纳显式校验后的数值。
- 将环境设备条件一览表映射到 `LIS_LIBDEF` 的温度、湿度、压力上下限；不把仪器 `ENVIRONMENT` 自由文本拆成结构化阈值。
- 维护环境条件项目与环境设备条件一览表当前采用只读形态。公共项目字典和实验室档案的写入责任、权限与审计没有确认前，不提供伪保存入口。
- 保留 Mock 文件作为显式回档材料，但正式模板不加载，真实查询失败或成功空结果均不自动切换 Mock。
- 相关前端已经同步到本地 Conda；JavaScript 语法和 SQL 占位符/注册参数数量已完成静态校验。

### 10.2 尚未完成且不得提前宣称的事项

- 7 个环境查询号仍需在目标平台注册或更新，并在服务启动后逐个验证返回标题、空结果和有数据结果。
- `LIS_DCIRCSLOG` 当前没有数据，厂家采集链路和外部接口尚未接通。
- 当前 33 条 `LIS_MCIRCSLOG` 不能归属现有实验室或仪器，不能用于页面演示真实监测值。
- `IFOK/FTYPE/FMODE` 的代码语义、项目单位和精度仍需厂家或业务字典确认。
- 实时状态、采集批次日志、通知推送、预警处置和远程控制没有由这五张表提供。

### 10.3 厂家条件具备后的调整顺序

1. 确认厂家设备或监测点与 `LIBSEQ/INSTID` 的稳定唯一关系，禁止按名称、地址尾号或页面顺序猜测。
2. 确认一次采集事务写入一条 `MCIRCSLOG` 和多条 `DCIRCSLOG`，明确幂等键、重复上报、失败回滚和断线补传。
3. 确认 `CIRCUITEMID/ITEMCODE`、单位、精度、`IFOK/FTYPE/FMODE`、设备时间和服务器时间口径。
4. 提供覆盖多对象、多项目、正常/异常、重复上报和补传的真实样例，先执行关联基数与机构隔离核验，再验证 7 个查询号。
5. 在代码字典确认后再开放中文状态、合格/不合格统计、阈值判断和对应筛选项。
6. 外部接口增加原始报文、接收日志、幂等结果和错误审计；实时状态、采集日志、预警闭环分别接各自真实数据源。
7. 完成本地 Conda、目标服务器和实际页面的分层验证，分别记录源码同步、SQL 注册、数据产生和接口联通状态。

后续验收报告必须分别说明：代码映射、SQL 注册、数据库明细、厂家接口、本地 Conda 和服务器部署。不能使用单一“已接入”覆盖这些不同完成状态。
