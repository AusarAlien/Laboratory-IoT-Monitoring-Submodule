# 实验室物联监控子模块开发指导与数据关联边界

本文档是本仓库后续前端页面、平台注册 SQL 和数据接入开发的统一业务边界。发生样例数据不完整、历史数据无法匹配或不同环境数据不一致时，优先保证本文确定的业务关系、数据流和机构隔离，不得根据少量历史数据临时改变字段语义。

## 1. 已确定的总体原则

- 本模块当前只开发前端页面、平台注册 SQL 和基本前后端交互，不新增 Java 接口。
- `LP_TBC_INSTFILE` 是 yncdc 功能平台的仪器设备业务主档。
- `INSTID` 是仪器设备业务主键；能耗表中的 `INSTID` 期望对应 `LP_TBC_INSTFILE.INSTID`。
- `FSSID` 是智能插座或采集终端编号，不能替代仪器业务主键。
- 页面以仪器、实验室、冷库、区域等业务对象为主，以插座、传感器、门禁控制器等物联设备为采集端。
- 真实查询使用左连接兼容无法关联档案的历史数据，不得因主档缺失丢弃采集记录。
- 当前业务范围不包含水浸设备、水浸指标和水浸模拟数据。
- Mock 只能作为显式关闭的降级代码保留，真实查询失败时不得静默切换 Mock。

## 2. 能耗业务模型

### 2.1 标识语义

| 标识 | 业务含义 | 用途 |
| --- | --- | --- |
| `LP_TBC_INSTFILE.INSTID` | 仪器设备业务主键 | 关联仪器名称、编号、型号、科室、位置和责任人 |
| 能耗表 `INSTID` | 应对应仪器设备主键 | 按仪器归集历史、状态和累计数据 |
| `FSSID` | 智能插座/采集终端编号 | 区分同一仪器下的具体采集终端 |
| `FID/FSEQ` | 数据记录主键 | 唯一标识一条历史或累计记录 |
| `FHIINO` | 所属机构 | 数据隔离和会话范围过滤 |

一台仪器可以绑定一个或多个智能插座：

```text
LP_TBC_INSTFILE.INSTID
  ├─ FSSID-1
  └─ FSSID-2
```

页面以 `INSTID` 对应的仪器为主对象，`FSSID` 作为插座编号单独展示。仪器级统计可汇总多个插座，插座级明细保留 `FSSID`。

### 2.2 表职责

| 业务表 | 职责 | 页面用途 |
| --- | --- | --- |
| `HTLIS.LP_TBC_INSTFILE` | 仪器设备主档 | 编号、名称、型号、科室、位置、责任人、档案状态 |
| `HII.IB_TBS_DEVICEMONITORLOG` | 能耗历史监测记录 | 采集明细、趋势、状态变化、历史查询 |
| `HII.IB_TBS_ITLASTINSTSTATUS` | 最新状态快照 | 最新电流、电压、功率、运行状态、最后通信时间、状态消息 |
| `HII.IB_TBS_SMARTSOCKETLOGSHOW` | 累计值展示 | 总能耗、总使用时长等累计指标 |

正确业务流：

```text
仪器设备主档
  -> 绑定 INSTID + FSSID
  -> 智能插座采集
  -> 历史记录 DEVICEMONITORLOG
  -> 最新状态 ITLASTINSTSTATUS
  -> 累计值 SMARTSOCKETLOGSHOW
  -> 状态监测、数据采集、查询统计和预警
```

### 2.3 历史数据兼容

当前能耗数据主要是其他库接入的 2025 年历史数据；当前仪器档案是相对真实的 yncdc 数据。现有样例匹配情况不能用于否定 `INSTID` 的业务关系。

已知现状仅作为兼容背景：

- 能耗历史记录：81 个设备，机构 `441200011`。
- 最新状态：82 个设备。
- 累计值：132 个设备。
- 当前样例仪器档案：10 条，机构为 `0`。
- 当前三类能耗数据与样例档案匹配数均为 0。

处理规则：

- SQL 继续按 `能耗表.INSTID = LP_TBC_INSTFILE.INSTID` 左连接。
- 匹配成功时展示正式仪器档案。
- 匹配失败时保留历史数据，编号使用能耗 `INSTID`，名称降级为“设备 + INSTID”，科室显示“未设置”。
- 不得根据名称、位置或相近编号猜测映射。
- 不得把 `FSSID` 改造成仪器档案主键。
- 不得为了适配样例数据批量改写历史 `INSTID`。

### 2.4 机构隔离

- 带 `FHIINO` 的历史表必须使用平台会话机构过滤。
- `ITLASTINSTSTATUS`、`SMARTSOCKETLOGSHOW` 没有 `FHIINO` 时，不得直接无范围返回全库数据。
- 最新状态和累计值应通过当前机构仪器档案的 `INSTID`，或通过已按机构过滤的历史设备集合限定范围。
- 前端不得自行传入或伪造机构编号覆盖平台会话。

## 3. 温湿度业务模型

| 业务表 | 职责 | 主要关联 |
| --- | --- | --- |
| `HII.IP_TBS_DEVICEIP` | 温湿度网络设备档案 | `FDEVICEIP` |
| `HTLIS.LIS_CHGSAMPDEPOT_REQCIRCU` | 温湿度历史记录 | `HTDEVICEIP -> FDEVICEIP` |
| `HII.IB_TBS_STANDARD` | 指标标准 | `FSDID=604` 温度，`FSDID=605` 湿度 |

边界：

- `FDEVICEIP` 是温湿度采集设备标识，不等同于仪器档案 `INSTID`。
- 温湿度设备需要映射到实验室、冷库、冰箱、培养箱、仪器等“环境监测对象”。
- 只有监测对象确实是仪器时，才关联 `LP_TBC_INSTFILE.INSTID`。
- 全局标准可用于基础判断，但每个监测对象的个性化上下限需要独立的环境条件要求数据。
- 当前温湿度记录表没有数据属于数据源现状，不得用 Mock 冒充真实采集记录。

## 3.1 环境预警业务模型

| 业务表 | 职责 | 主要关联 |
| --- | --- | --- |
| `HII.IP_TBS_MONITORALARM` | 环境监测报警事件 | `FSCODE` 为历史传感器节点编码 |
| `HII.IP_TBS_DEVICEIP` | 环境监测设备档案 | `FDEVICEIP` 的 `#` 后缀为节点号 |
| `HII.IB_TBS_STANDARD` | 环境监测指标字典 | `FITEMNO -> FSDID` |

已通过实际数据确认，历史报警的设备关系不是 `FSCODE=FDEVICEIP`，而是：

```text
IP_TBS_MONITORALARM.FSCODE 去除前导 0
  -> IP_TBS_DEVICEIP.FDEVICEIP 中“#”后的节点号去除前导 0
```

当前验证数据中，`000061/000062/000064/000070` 均能唯一匹配一个 `FTYPE='HT'` 的环境设备，合计覆盖 597 条报警且无一对多关系。查询必须先按会话机构限定 `IP_TBS_DEVICEIP`，再匹配报警；不得直接使用报警表中遗留的机构编码扩大查询范围。若同一机构内节点号出现多个设备，必须视为关系不唯一并停止自动匹配，不能任取一条。

当前报警数据属于 2023 年历史记录，只有 `FALARMTYPE=2` 的异常值报警，指标为二氧化碳（601）、湿度（605）和 TVOC（606）。它可以支持历史报警列表，不得据此声称存在当前实时报警、温度报警、通知结果或完整处置流程。

后续新产生的报警应直接保存完整 `FDEVICEIP` 或稳定设备唯一键，节点尾号匹配只作为现有历史数据的兼容规则。

## 4. 门禁业务模型

```text
IB_TBS_USERINFO.USERID
  -> IB_TBS_CHECKINOUT.USERID

IB_TBS_LABAREA.SN
  -> IB_TBS_CHECKINOUT.SN
```

| 业务表 | 职责 |
| --- | --- |
| `HII.IB_TBS_USERINFO` | 门禁人员、卡号和基础权限信息 |
| `HII.IB_TBS_LABAREA` | 门禁控制器、区域和进出方向配置 |
| `HII.IB_TBS_CHECKINOUT` | 门禁出入事件记录 |

边界：

- `USERID` 是人员与出入记录的关联键。
- `SN` 是门禁控制器与区域记录的关联键。
- 门禁控制器通常属于安全设施，不强制关联仪器档案。
- 门禁记录查询不等于门禁权限下发；人员授权、控制器配置、下发结果需要相应真实业务表或接口。
- `IB_TBS_USERINFO` 在本模块中只作为门禁识别人员和出入事件的查询数据，不作为人员主档维护入口。
- 人员新增、编辑、停用、人脸维护等人员配置由外部人员管理系统负责，本模块不得重复建设写入功能。
- 门禁管理以实验室和门点远程监控为主。现有出入记录只能证明历史活动，不能推导控制器在线状态、门开关状态或视频状态。
- 远程开锁、呼叫、视频必须由可信设备网关或平台接口提供，并记录操作人、门点、指令时间和执行结果；接口缺失时页面只能展示未启用的能力入口，不能模拟成功。

## 5. 页面数据源边界

| 页面 | 主数据源 | 边界 |
| --- | --- | --- |
| 物联配置管理 | 仪器主档、物联设备配置和绑定关系 | 主档不能替代 IP、端口、周期、账号、权限配置 |
| 数据采集管理 | 温湿度历史、能耗历史 | 采集执行日志需要独立日志数据，不能由监测记录代替 |
| 物联状态监测 | 能耗最新状态及其他设备心跳/状态 | 档案 `STATUS` 不能代替实时运行状态 |
| 物联查询统计 | 历史记录、累计值、仪器主档 | 趋势使用历史表，累计结果使用累计表 |
| 监测预警管理 | 阈值规则、预警事件、处理和推送记录 | 普通监测日志不能替代预警业务表 |
| 环境条件采集 | 温湿度设备和历史记录 | 采集设备必须明确对应环境监测对象 |
| 环境设备条件一览表 | 监测对象与环境项目要求 | 全局标准不能替代对象级条件要求 |
| 环境记录 | 温湿度历史及业务抓取记录 | 自动采集和检验时抓取需保留来源 |
| 智能预警 | 环境规则、事件、通知和处置记录 | 声光、短信、客户端、平台推送需要真实结果记录 |
| 门禁管理 | 人员、区域、控制器、出入记录及远程操作审计 | 以实验室门点监控为主；历史事件不能冒充实时状态，查询数据不能冒充远程控制能力 |
| 一卡通管理 | 外部人员主档及门禁、访客等人员活动记录 | 一卡通是人员活动数据卡和跨系统查询视图，不在本模块新增、编辑人员或维护卡片与权限 |

## 6. 禁止事项

- 禁止因为当前老数据无法匹配就把 `FSSID` 定义为仪器主键。
- 禁止使用内连接导致无档案的历史采集记录丢失。
- 禁止把仪器档案状态当作在线、离线、关机等实时状态。
- 禁止把累计值逐条求和；累计增量应按合理时间范围计算最大值减最小值。
- 禁止无机构范围查询缺少 `FHIINO` 的最新状态和累计表。
- 禁止用普通采集记录模拟采集批次日志、预警事件或推送结果。
- 禁止在页面出现水浸相关设备、指标、筛选项和 Mock 数据。
- 禁止在真实查询失败时自动切换到 Mock。

## 7. 后续开发验收检查

每次新增或修改数据接入至少检查：

1. 页面主对象是仪器/实验室/区域，还是采集终端，语义是否明确。
2. `INSTID`、`FSSID`、`FDEVICEIP`、`SN`、`USERID` 是否按本文边界使用。
3. 是否保留机构会话隔离。
4. 主档缺失时是否保留采集历史并安全降级。
5. 历史、最新状态、累计值是否使用正确的数据表。
6. 页面指标名称、单位和纵坐标是否与实际字段一致。
7. 是否误把档案状态、采集状态、预警状态混为一类。
8. 是否仍有水浸内容或隐式 Mock 回退。
9. SQL、前端字段映射和页面筛选是否一致。
10. 源码同步到本地 Conda 后是否完成实际入口验证。
